/**
 * ChatBI URL 工具模块
 * Copyright © 2025-2026 Felix Alvin Juandra (蔡威广)
 *
 * 格式化 URL 路径
 * @param url URL 路径
 * @returns 格式化后的 URL
 */
export const formatUrl = (url: string) => {
  return url
}

// 向后兼容别名
export const formatDataEaseBi = formatUrl
export const formatChatBIUrl = formatUrl
